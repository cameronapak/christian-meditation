---
import { getCollection } from "astro:content";
import Layout from "../../layouts/Layout.astro";
import Logo from "../../components/Logo.astro";

const pageTitle = "My Blog";
const allPosts = await getCollection("posts");
---

<Layout title={pageTitle}>
  <main class="container p-6 outline-none">
    <div class="flex flex-col gap-2 sticky top-0 py-2 bg-white z-20 dark:bg-slate-800">
      <Logo />
    </div>
    <div class="text-sm breadcrumbs">
      <ul class="!p-0 !m-0">
        <li><a href="/" class="dark:text-slate-200">Home</a></li>
        <li>Posts</li>
      </ul>
    </div>

    <ul class="dark:text-slate-200 dark:fill-slate-100">
      {
        allPosts.map((post) => (
          <li class="dark:text-slate-200 ">
            <a class="dark:text-slate-200" href={`/posts/${post.slug}`}>{post.data.title}</a>
          </li>
        ))
      }
    </ul>
  </main>
</Layout>
